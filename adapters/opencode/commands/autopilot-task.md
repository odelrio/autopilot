---
description: Execute one roadmap item end-to-end — claim, optional spec gate, implement, verify, review ritual, open PR, merge, bookkeeping.
---

Execute one roadmap item end-to-end. Invoked as `/autopilot-task <item-id>`. Load
**`autopilot-standards`** first, then drive the item through the full ritual:
pick & open → spec gate (if configured) → implement → verify → reviews → PR & merge →
bookkeeping. Follow the stop conditions and per-session digest rules.
